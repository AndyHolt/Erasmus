% File name: %{{ meta.filename %}}
% %{{ meta.passagename %}}
% Generated by Erasmus.
% Date: %{{ meta.datestamp %}}

\documentclass{polyglot}

% Define the title
\title{%{{ meta.passagename %}}}

\begin{document}

% generate title
\maketitle

\begin{paracol}{%{{ meta.no_of_translations %}}}
%{ for translation in translations %}
    % %{{ translation.name %}}
    %{{ translation.text|wordwrap(width=75,break_long_words=false)|indent %}}
    %{ if not loop.last %}
    \switchcolumn
    %{ endif %}
%{ endfor %}
\end{paracol}

\end{document}
