% File name: %{{ meta.filename %}}
% %{{ meta.passagename %}}
% Generated by Erasmus.
% Date: %{{ meta.datestamp %}}

\documentclass{polyglot}

% Define the title
\title{%{{ meta.passagename %}}}

\begin{document}

% generate title
\maketitle

\begin{paracol}{%{{ meta.no_of_translations %}}}
%{ for translation in meta.translations %}
    {\centering\underline{\bfseries %{{ translation %}} }\par}
    %{ if not loop.last %}
    \switchcolumn
    %{ else %}
    \switchcolumn*
    %{ endif %}
%{ endfor %}
%{ for paragraph in aligned_paragraphs %}
%{ set outer_loop = loop %}
%{ for translation in paragraph %}
    % %{{ meta.translations[loop.index0] %}}
    %{{ translation|wordwrap(width=75,break_long_words=false)|indent %}}
    %{ if not loop.last %}
    \switchcolumn
    %{ elif not outer_loop.last %}
    \switchcolumn*
    %{ endif %}
%{ endfor %}
%{ endfor %}
\end{paracol}

\end{document}
